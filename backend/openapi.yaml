# Generated with protoc-gen-openapi
# https://github.com/google/gnostic/tree/master/cmd/protoc-gen-openapi

openapi: 3.0.3
info:
    title: Greeter API
    description: The greeting service definition.
    version: 0.0.1
paths:
    /me:
        get:
            tags:
                - Greeter
            description: Sends a greeting
            operationId: Greeter_UserInformation
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/user.v1.UserInformationResponse'
components:
    schemas:
        user.v1.UserInformationResponse:
            type: object
            properties:
                name:
                    type: string
            description: The response message containing the greetings
tags:
    - name: Greeter
